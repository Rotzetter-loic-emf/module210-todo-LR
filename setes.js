module.exports = async function (context, req) { try { const url = process.env.BACKEND_URL; if (!url) { context.res = { status: 500, body: { error: "BACKEND_URL missing" } }; return; } const r = await fetch(url); if (!r.ok) { context.res = { status: r.status, body: { error: "Backend error", status: r.status } }; return; } const tasks = await r.json(); const count = Array.isArray(tasks) ? tasks.length : 0; context.res = { status: 200, headers: { "Content-Type": "application/json", "Access-Control-Allow-Origin": "*" }, body: { count } }; } catch (e) { context.res = { status: 500, body: { error: String(e) } }; } };